name: localization_table
description: A new Flutter package.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  csv: ^4.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: